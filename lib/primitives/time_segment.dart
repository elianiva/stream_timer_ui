enum SegmentType {
  hours,
  minutes,
  seconds,
  unknown,
}

enum SegmentDigitType {
  secondsTens,
  secondsOnes,
  minutesTens,
  minutesOnes,
  hoursTens,
  hoursOnes,
  unknown
}
